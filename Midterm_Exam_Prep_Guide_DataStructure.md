# 🎯 คู่มือเจาะลึกแนวข้อสอบกลางภาค: วิชาโครงสร้างข้อมูลและอัลกอริทึม (Data Structure and Algorithm)
## ฉบับเก็งตรงจุดตามโครงสร้างข้อสอบจริง (ตอนที่ 1 และ ตอนที่ 2)

---

## 📌 สรุปรูปแบบข้อสอบกลางภาค (Exam Structure)

* **ตอนที่ 1 (50 ข้อ - ทำลงในกระดาษคำตอบ)**: ปรนัย/เลือกตอบ ครอบคลุมเนื้อหาบทที่ 1 - 5
* **ตอนที่ 2 (5 ข้อ - ทำลงในข้อสอบ)**: อัตนัย/แสดงวิธีทำและเขียนคำตอบ 5 ข้อใหญ่

---

## 📝 เจาะลึกและเตรียมตัวสอบ ตอนที่ 2 (ข้อสอบอัตนัย 5 ข้อใหญ่)

### 🔹 ข้อ 1: การเขียนสัญลักษณ์ผังงาน (Flowchart Symbols)
**โจทย์**: ให้เขียนสัญลักษณ์ของผังงานตามข้อความ/หน้าที่การทำงานที่กำหนดให้

| สัญลักษณ์ (Symbol Name) | รูปทรง (Shape) | หน้าที่ / ความหมาย |
| :--- | :--- | :--- |
| **Terminator** | วงกลมมน / สี่เหลี่ยมมุมมน | จุดเริ่มต้น (START) หรือ สิ้นสุด (STOP) ของผังงาน |
| **Process** | สี่เหลี่ยมผืนผ้า | การประมวลผล, การคำนวณ (`Sum = A + B`), กำหนดค่าตัวแปร |
| **Input / Output** | สี่เหลี่ยมด้านขนาน | การรับข้อมูล (READ) หรือ แสดงผลข้อมูล (WRITE) โดยไม่ระบุสื่อ |
| **Decision** | สี่เหลี่ยมขนมดียกปูน | การเปรียบเทียบ/ตัดสินใจเลือกทำตามเงื่อนไข (IF / CASE) |
| **Manual Input** | สี่เหลี่ยมคางหมูทรงเอียง | การรับข้อมูลเข้าทางแป้นพิมพ์ (Keyboard) |
| **Display** | รูปทรงจอภาพ | การแสดงผลลัพธ์ออกทางจอภาพ |
| **Printer** | รูปทรงเอกสารพริ้นเตอร์ | การแสดงผลลัพธ์ออกทางเครื่องพิมพ์ (Printer) |
| **Preparation** | รูปหกเหลี่ยม | การกำหนดค่าล่วงหน้าสำหรับการทำงานซ้ำ (For Loop) |
| **On-Page Connector** | วงกลมเล็ก | จุดเชื่อมต่อผังงานภายในหน้าเดียวกัน |
| **Off-Page Connector** | รูปห้าเหลี่ยมตัดมุมลง | จุดเชื่อมต่อผังงานที่อยู่คนละหน้า |
| **Flow Line** | เส้นลูกศร | ทิศทางและลำดับขั้นตอนการทำงาน |

---

### 🔹 ข้อ 2: การวิเคราะห์ประสิทธิภาพ Big-O จาก Pseudo Code
**โจทย์**: ให้เติมตารางวิเคราะห์ลำดับคำสั่ง, จำนวนครั้งที่ทำงาน, สรุปรวม $T(n)$ และค่า Time Complexity $= O(...)$

#### 💡 โครงสร้างตารางคำนวณ (Template):
| ลำดับคำสั่ง | คำสั่ง (Statement) | จำนวนครั้งที่ทำงาน | คำอธิบาย |
| :---: | :--- | :---: | :--- |
| **1** | `READ N` | $1$ | ทำงาน 1 ครั้ง |
| **2** | `SET Sum = 0` | $1$ | ทำงาน 1 ครั้ง |
| **3** | `LOOP (i = 0 to N)` | $n + 1$ | ทำงาน $n+1$ ครั้ง (รวมรอบพิจารณาออกลูป) |
| **3.1** | `Sum = Sum + i` | $n$ | ทำงานในลูป $n$ ครั้ง |
| **4** | `END LOOP` | - | ไม่นำมาคิด |
| **5** | `WRITE Sum` | $1$ | ทำงาน 1 ครั้ง |

* **รวมจำนวนครั้งที่ทำงาน $T(n)$**:
  $$T(n) = 1 + 1 + (n + 1) + n + 1 = 2n + 4$$
* **ค่า Time Complexity**:
  $$	ext{Time Complexity} = O(n)$$ *(ตัดค่าคงที่และเลือกพจน์ที่เติบโตเร็วที่สุด)*

#### ⚠️ ข้อควรจำสำหรับวิเคราะห์ Big-O:
* คำสั่งทั่วไป (`SET`, `READ`, `WRITE`, `IF`) ทำงาน $1$ ครั้ง
* ลูปเชิงเส้นแบบ `0 ถึง N` ทำตัววนลูป $n+1$ ครั้ง และคำสั่งข้างในลูปทำ $n$ ครั้ง
* สรุปค่า Big-O จาก $T(n)$:
  * ถ้า $T(n) = c$ (ค่าคงที่) $ightarrow O(1)$
  * ถ้า $T(n) = an + b$ $ightarrow O(n)$
  * $T(n) = an^2 + bn + c$ $ightarrow O(n^2)$
  * $T(n) = a \log n + b$ $ightarrow O(\log n)$

---

### 🔹 ข้อ 3.1: การคำนวณหาแอดเดรสอาร์เรย์ 1 มิติ (หน่วยเป็นบิต!)
**โจทย์**: คำนวณหาแอดเดรส $A[i]$ โดยกำหนด Base Address ($B$), Lower Bound ($L$), ขนาดของแต่ละช่อง ($C$) ในหน่วย **บิต (Bits)**

#### 📐 สูตรคำนวณ:
$$	ext{Loc}(A[i]) = B + C_{	ext{byte}} 	imes (i - L)$$

#### ⚠️ กับดักสำคัญเรื่องหน่วย (Bits vs Bytes):
* ถ้าโจทย์ระบุขนาดช่องเป็น **บิต (Bits)** แต่ Base Address ($B$) เป็นแอดเดรสหน่วยความจำแบบ **ไบต์ (Bytes)**
* **ต้องแปลงบิตเป็นไบต์ก่อน**:
  $$C_{	ext{byte}} = rac{C_{	ext{bits}}}{8}$$
  *(เช่น 40 บิต $= 40 / 8 = 5$ ไบต์, 80 บิต $= 80 / 8 = 10$ ไบต์, 16 บิต $= 16 / 8 = 2$ ไบต์)*

#### 💡 ตัวอย่างคำนวณ:
> **โจทย์**: จงคำนวณหาแอดเดรส $A[5]$ โดยกำหนดให้ $A[0]$ บันทึกที่ตำแหน่ง 1500 และแต่ละช่องมีขนาด 40 บิต
> * **วิเคราะห์ข้อมูล**: $B = 1500$, $L = 0$, $i = 5$, $C = 40 	ext{ บิต} = rac{40}{8} = 5 	ext{ ไบต์}$
> * **แทนค่าในสูตร**:
>   $$	ext{Loc}(A[5]) = 1500 + 5 	imes (5 - 0) = 1500 + 25 = 1525$$

---

### 🔹 ข้อ 3.2: การคำนวณหาแอดเดรสอาร์เรย์ 2 มิติ (Row Major & Column Major)
**โจทย์**: กำหนดอาร์เรย์ $A$ ขนาด $5 	imes 10$ ($M 	imes N$) เริ่มต้นที่ $A[1, 1]$ อยู่ที่แอดเดรส $B$ แต่ละช่องกินเนื้อที่ $C$ Bytes จงหาแอดเดรสของ $A[i, j]$ ทั้งแบบ Row Major และ Column Major

#### 📐 สูตรคำนวณ:
1. **แบบ Row Major Order (เรียงตามแถวเป็นหลัก)**:
   $$	ext{Loc}(A[i, j]) = B + C 	imes \left[(i - L_1) 	imes N + (j - L_2)ight]$$
2. **แบบ Column Major Order (เรียงตามคอลัมน์เป็นหลัก)**:
   $$	ext{Loc}(A[i, j]) = B + C 	imes \left[(j - L_2) 	imes M + (i - L_1)ight]$$

* *หมายเหตุ*:
  * $M$ = จำนวนแถวทั้งหมด $= 5$
  * $N$ = จำนวนคอลัมน์ทั้งหมด $= 10$
  * $L_1 = 1, L_2 = 1$ (เนื่องจากโจทย์ระบุเริ่มต้นที่ $A[1, 1]$)

#### 💡 ตัวอย่างคำนวณ:
> **โจทย์**: อาร์เรย์ $A$ ขนาด $5 	imes 10$ เมื่อ $A[1, 1]$ อยู่ที่แอดเดรส 1050 แต่ละช่องใช้พื้นที่ 4 Bytes จงหาแอดเดรสของ $A[3, 5]$
> * **ข้อมูล**: $B = 1050, C = 4, M = 5, N = 10, L_1 = 1, L_2 = 1, i = 3, j = 5$
> * **1. Row Major Order**:
>   $$	ext{Loc}(A[3, 5]) = 1050 + 4 	imes \left[(3 - 1) 	imes 10 + (5 - 1)ight] = 1050 + 4 	imes (20 + 4) = 1050 + 96 = 1146$$
> * **2. Column Major Order**:
>   $$	ext{Loc}(A[3, 5]) = 1050 + 4 	imes \left[(5 - 1) 	imes 5 + (3 - 1)ight] = 1050 + 4 	imes (20 + 2) = 1050 + 88 = 1138$$

---

### 🔹 ข้อ 4: การแปลงนิพจน์ Infix เป็น Postfix และหาผลลัพธ์
**โจทย์**:
1. แปลงนิพจน์ Infix ให้เป็น Postfix โดยแสดงตารางขั้นตอนการใช้ Stack
2. หาผลลัพธ์ตัวเลขของนิพจน์ Postfix นั้น

#### 1. กฎการแปลง Infix $ightarrow$ Postfix
* ลำดับความสำคัญ Operator: `()` > `^` (3) > `*`, `/` (2) > `+`, `-` (1)
* อ่าน Input จากซ้ายไปขวา:
  * **Operand (ตัวเลข/ตัวแปร)** $ightarrow$ ส่งออกไปที่ Output
  * **Operator** $ightarrow$ เปรียบเทียบกับตัวบนสุดใน Operator Stack (`opst`):
    * ถ้า Input มี Precedence **สูงกว่า** ตัวบนสุดใน `opst` $ightarrow$ PUSH ลง `opst`
    * ถ้า Input มี Precedence **น้อยกว่าหรือเท่ากับ** ตัวบนสุดใน `opst` $ightarrow$ POP ตัวใน `opst` ออกไป Output จนกว่าจะเจอตัวที่เล็กกว่า แล้วค่อย PUSH Input ลงไป
  * **วงเล็บเปิด `(`** $ightarrow$ PUSH ลง `opst`
  * **วงเล็บปิด `)`** $ightarrow$ POP ออกไป Output จนกว่าจะเจอ `(` (แล้วทิ้งวงเล็บทั้งคู่)

#### 2. กฎการหาผลลัพธ์จาก Postfix
* อ่านนิพจน์ Postfix จากซ้ายไปขวา:
  * เจอ **Operand** $ightarrow$ PUSH ลง Stack
  * เจอ **Operator** $ightarrow$ POP ข้อมูลออก 2 ตัว ($Op2$ ออกมาก่อน, $Op1$ ออกตามหลัง)
  * คำนวณ $	ext{Result} = Op1 	ext{ Operator } Op2$ แล้ว PUSH ผลลัพธ์กลับเข้า Stack

---

### 🔹 ข้อ 5: การทำงานของลิงค์ลิสต์คู่แบบเรียงลำดับ (Doubly Linked List Sorting)
**โจทย์**: ให้เติมข้อมูลการทำงานของ Doubly Linked List แบบเรียงลำดับข้อมูลจาก **มากไปน้อย (Descending Order)**

#### 🧩 โครงสร้างโหนดลิงค์ลิสต์คู่:
$$	ext{[ LLINK / PREV } | 	ext{ DATA / INFO } | 	ext{ RLINK / NEXT ]}$$
* `LLINK` (Prev): ตัวชี้ไปยังโหนดก่อนหน้า (โหนดแรกสุด `LLINK = NULL`)
* `RLINK` (Next): ตัวชี้ไปยังโหนดถัดไป (โหนดสุดท้าย `RLINK = NULL`)

#### 🔄 หลักการแทรกข้อมูลแบบเรียงลำดับมากไปน้อย:
1. **แทรกเป็นโหนดแรก (ถ้าค่าใหม่ $> DATA(Head)$)**:
   * `Next(Newnode) = Head`
   * `Prev(Newnode) = NULL`
   * `Prev(Head) = Newnode`
   * `Head = Newnode`
2. **แทรกระหว่างโหนด P และโหนดถัดไป**:
   * `Prev(Next(P)) = Newnode`
   * `Next(Newnode) = Next(P)`
   * `Prev(Newnode) = P`
   * `Next(P) = Newnode`
3. **แทรกต่อท้ายสุด (ถ้าค่าใหม่ $< DATA(Tail)$)**:
   * `Next(Newnode) = NULL`
   * `Prev(Newnode) = Tail`
   * `Next(Tail) = Newnode`
   * `Tail = Newnode`

---

## 📖 สรุปเก็งเนื้อหาตอนที่ 1 (ปรนัย 50 ข้อ บทที่ 1 - 5)

1. **บทที่ 1**:
   * สมการโปรแกรม ($	ext{Data} + 	ext{Algorithm} = 	ext{Program}$)
   * Data Hierarchy (Bit $ightarrow$ Byte $ightarrow$ Field $ightarrow$ Record $ightarrow$ File $ightarrow$ Database)
   * Big-O Growth Rates: $O(1) < O(\log n) < O(n) < O(n \log n) < O(n^2) < O(2^n) < O(n!)$
2. **บทที่ 2**:
   * คุณสมบัติอาร์เรย์: Homogeneous, Contiguous Memory, Random Access ($O(1)$), Static Size
3. **บทที่ 3**:
   * สแตก: **LIFO**, ทำงานที่ `TOP`
   * Push (เช็ค **Overflow** เมื่อ `TOP == MAX`), Pop (เช็ค **Underflow** เมื่อ `TOP == 0`)
4. **บทที่ 4**:
   * คิว: **FIFO**, เพิ่มที่ `REAR`, ลบที่ `FRONT`
   * คิวเชิงเส้นมีปัญหา **False Overflow** $ightarrow$ แก้ด้วย **Circular Queue** (`(Rear + 1) % Size == Front`)
5. **บทที่ 5**:
   * ลิงค์ลิสต์: Dynamic Memory, ใช้ Pointer ชี้ต่อกัน, `AVAIL` คือกลุ่มโหนดว่าง
   * โหนดสุดท้ายชี้ `NULL`
